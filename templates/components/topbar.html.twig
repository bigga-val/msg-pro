<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button">
                <i class="fas fa-bars"></i>
            </a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="{{ path('app_landing') }}" class="nav-link">
                <i class="fas fa-globe me-1"></i> Voir le site
            </a>
        </li>
    </ul>

    <ul class="navbar-nav ml-auto align-items-center">
        {% if app.user %}
            <li class="nav-item me-2">
                <span class="topbar-sms-badge">
                    <i class="fas fa-sms me-1"></i>
                    <span id="qty-sms-top">{{ app.user.totalSMS ?? 0 }}</span> SMS restants
                </span>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_commande') }}" class="btn btn-sm btn-primary btn-topbar-recharge">
                    <i class="fas fa-plus me-1"></i> Recharger
                </a>
            </li>
            <li class="nav-item">
                <span class="topbar-username">
                    {{ app.user.username | upper }}
                </span>
                <a href="{{ path('app_logout') }}" class="topbar-logout">
                    Déconnexion
                </a>
            </li>
        {% endif %}
    </ul>
</nav>
